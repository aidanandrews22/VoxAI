import type {
  ExtendedNotebookFile,
  SidebarProps,
  FolderItemProps,
  FileListItemProps,
  ChatSessionItemProps,
  FolderViewProps,
  NotebookViewProps,
} from "../../types/sidebar";

// Re-export all the types for use in other components
export type {
  ExtendedNotebookFile,
  SidebarProps,
  FolderItemProps,
  FileListItemProps,
  ChatSessionItemProps,
  FolderViewProps,
  NotebookViewProps,
};
